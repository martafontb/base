{%- assign config = block.settings -%}

<{{ config.tag }} class="block--heading {{ config.size }}" {{ block.shopify_attributes }}>
  {{ config.heading }}
</{{ config.tag }}>

{%- schema -%}
  {
    "name": "Heading",
    "tag": null,
    "settings": [
      {
        "type": "header",
        "content": "Content"
      },
      {
        "type": "text",
        "id": "heading",
        "label": "Heading"
      },
      {
        "type": "header",
        "content": "Style"
      },
      {
        "type": "select",
        "id": "size",
        "label": "Size",
        "options": [
          { "label": "Primary heading", "value": "h1" },
          { "label": "Secondary heading", "value": "h2" },
          { "label": "Tertiary heading", "value": "h3" }
        ],
        "info": "Controls the appearance of the heading."
      },
      {
        "type": "select",
        "id": "tag",
        "label": "Type",
        "options": [
          { "label": "Page Heading", "value": "h1" },
          { "label": "Section Heading", "value": "h2" },
          { "label": "Subheading", "value": "h3" },
          { "label": "Sub-subheading", "value": "h4" }
        ],
        "info": "Sets the importance of the heading in the document hierarchy – this is very important for SEO and accessibility.",
        "default": "h2"
      }
    ],
    "presets": [
      {
        "name": "Page Heading",
        "settings": {
          "heading": "Heading",
          "size": "h1",
          "tag": "h1"
        }
      },
      {
        "name": "Section Heading",
        "settings": {
          "heading": "Heading",
          "size": "h2",
          "tag": "h2"
        }
      },
      {
        "name": "Subheading",
        "settings": {
          "heading": "Heading",
          "size": "h3",
          "tag": "h3"
        }
      }
    ]
  }
{%- endschema -%}